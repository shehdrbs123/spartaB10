using System;
public class Solution {
    public long solution(long n) {
        long answer = 0;
        
        if(n % Math.Sqrt(n)  == 0)
            answer = (long)Math.Pow(Math.Sqrt(n)+1, 2);
        else
            return -1;
        
        return answer;
    }
}
